name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: Verify
    uses: maveniverse/parent/.github/workflows/ci.yml@release-62
    with:
      jdk-matrix: '[ "17", "21", "25" ]'
      maven-test: './mvnw clean verify -e -B -V -P run-its -f it'

